# Workflow CI

Folder ini digunakan untuk dokumentasi tambahan dan konfigurasi workflow CI dalam proyek.

`Workflow-CI/MLProject` menyimpan file `modelling.py`, `conda.yaml`, dan `MLProject` untuk penggunaan MLflow Project.

Gunakan `.github/workflows/main.yml` di root repository untuk menjalankan pipeline CI otomatis.
